<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipChildren="false"
    android:clipToPadding="false">

    <!-- Fondo: cielo con nubes (más ancho que la pantalla para que se pueda desplazar) -->
    <ImageView
        android:id="@+id/fondoCielo"
        android:layout_width="2000dp"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:scaleType="centerCrop"
        android:src="@drawable/img" />

    <!-- Oscurecido sutil para que el texto blanco se lea bien -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#22000000" />

    <!-- Contenido central -->
    <LinearLayout
        android:id="@+id/contenido"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:alpha="0"
        android:gravity="center"
        android:orientation="vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:letterSpacing="0.08"
            android:shadowColor="#66000000"
            android:shadowDx="2"
            android:shadowDy="3"
            android:shadowRadius="8"
            android:text="MI CLIMA"
            android:textColor="#FFFFFF"
            android:textSize="38sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="14dp"
            android:letterSpacing="0.04"
            android:shadowColor="#66000000"
            android:shadowDx="1"
            android:shadowDy="2"
            android:shadowRadius="6"
            android:text="Consultando el clima..."
            android:textColor="#EAF6FF"
            android:textSize="17sp" />
    </LinearLayout>

</FrameLayout>